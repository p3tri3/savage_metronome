[package]
name = "metronome"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.27"
egui = "0.27"
rodio = { version = "0.17", optional = true }

[features]
default = ["audio"]
audio = ["dep:rodio"]
